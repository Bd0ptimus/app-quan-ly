<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">i-TechCo</a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <div class="container-fluid d-flex flex-column flex-lg-row justify-content-lg-between">
        <div class="d-flex ml-auto">
          <ul class="navbar-nav">
            <li class="nav-item">
              <a class="nav-link" href="/">Home</a>
            </li>
          </ul>
        </div>
        <div class="d-flex ml-auto">
          <ul class="navbar-nav">
            <% if(currentUser){ %>
            <li class="nav-item">
              <form action="/logout" method="POST" id="myform">
                <a
                  href="#"
                  class="nav-link"
                  onclick="document.querySelector('#myform').submit()"
                  >Log out</a
                >
              </form>
            </li>
            <!-- </div> -->
            <% } %>
          </ul>
        </div>
      </div>
    </div>
  </div>
</nav>
